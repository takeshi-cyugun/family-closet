# ファミリークロゼット アプリ システム要件定義書 (V10 完全版)

## 1. プロジェクト概要

本システムは、家族単位で洋服の所有状況・状態（使用中、保管中、譲渡/廃棄予定など）を共有・管理するためのグローバル対応Web/モバイルアプリケーション、および運営企業向けのアプリ全体の管理画面（App Admin）です。

- **アプリ名称（仮）：** ファミリークロゼット
- **主要ターゲット：** 一般ファミリー層（グローバル展開）、アプリ運営者
- **提供プラットフォーム：** Webブラウザ、iOS、Android（WebアプリをNative WebViewでラップして配信）、Web管理画面

---

## 2. モノレポ・バックエンド構成およびアーキテクチャ方針

本プロダクトは **Turborepo + pnpm** によるモノレポ構成を採用し、一般ユーザー向けアプリと運営者向け管理画面を完全に分離された個別のアプリケーションとして構築します。

### 2.1 ディレクトリ構成および役割定義

```text
family-closet/ (モノレポルート)
├── apps/
│   ├── web/                      # ユーザー向け Web / WebView アプリ (Next.js App Router)
│   │   ├── app/                  # クリーンなURLルーティング (パターンB: /register, /dashboard 等)
│   │   │   ├── page.tsx          # 画面① LP兼ゲスト体験
│   │   │   ├── register/         # 画面② ファミリー新規登録
│   │   │   ├── dashboard/        # 画面④ 洋服一覧
│   │   │   ├── actions/          # Server Actions (一般ユーザー用バックエンド処理)
│   │   │   └── api/              # Route Handlers (Gemini API, Webhook等)
│   │   └── next.config.js
│   │
│   └── admin/                    # 運営者向け 管理画面アプリ (Next.js App Router)
│       ├── app/
│       │   ├── dashboard/        # 運営ダッシュボード
│       │   ├── audit-logs/       # 監査ログ閲覧画面
│       │   ├── actions/          # Server Actions (運営特権用バックエンド処理)
│       │   └── api/              # Route Handlers (管理専用API)
│       └── next.config.js
│
└── packages/
    ├── database/                 # Supabase クライアント / Drizzle ORM スキーマ / 型定義
    │   └── src/
    │       ├── client.ts         # DB接続クライアント生成
    │       ├── schema.ts         # Drizzle ORM スキーマ定義
    │       └── index.ts          # パッケージエントリポイント
    ├── ui/                       # 共通 UI コンポーネント (shadcn/ui, Tailwind CSS)
    ├── i18n/                     # 多言語辞書データ (日本語・英語・簡体字・繁体字) / 判定ユーティリティ (Cookie/DB参照)
    └── config/                   # 共通設定 (ESLint / TypeScript / Tailwind)

```

### 2.2 バックエンドの分離とセキュリティ境界

Next.js App Router の特性を活かし、`apps/web` と `apps/admin` のそれぞれの `app/` ディレクトリ配下に独立したバックエンド処理（Server Actions / Route Handlers）を配置します。

- **`apps/web` バックエンド:**
- **Server Actions:** ゲストセッション発行、ファミリー新規登録、ログイン・認証処理、ゲストから正式アカウントへのデータ移管。
- **Route Handlers:** Google Gemini（`gemini-3.1-flash-lite`）を呼び出す画像解析API、Stripe / RevenueCat の決済通知処理。画像解析APIは呼び出し時に表示言語を渡し、AIが返す品名・カテゴリ・色・シーズンをその言語で受け取る。
- **アクセス権限:** Supabase Row Level Security (RLS) により、ログイン中のユーザーが所属する `family_id` のデータのみアクセス可能。

- **`apps/admin` バックエンド:**
- **Server Actions:** ファミリーアカウント凍結・解約、サブスクリプションプランの手動変更、不可逆な操作履歴の記録。
- **Route Handlers:** 管理者専用データ出力API。
- **アクセス権限:** サービスロールキー（特権アクセス権限）の利用を `apps/admin` 内のバックエンドのみに限定し、一般アプリからの特権操作呼び出しリスクを根本から排除。

### 2.3 メール送信基盤

- **ドメイン：** `family-closet.com`（ムームードメインで取得）。
- **認証系メール（本人確認メール等）の送信元：** `noreply@family-closet.com`。Supabase Auth のメール送信を、Resend（https://resend.com）のSMTP中継（`smtp.resend.com`）を使ったカスタムSMTP設定（Supabase Dashboard の Authentication → Emails → SMTP Settings）に切り替えて送信する。`family-closet.com` をResend側で送信ドメインとして検証済みにしておけば、専用メールボックスを新規作成する必要はなく、そのドメイン上の任意のローカルパート（`noreply@`等）をFromに使える。
  - **経緯：** 当初はさくらインターネットのSMTPを中継先として採用していたが、Supabase（海外の送信元IP）からのSMTP接続がタイムアウトし、`auth.signUp()` が `AuthRetryableFetchError`（HTTP 504）で失敗する事象が本番環境で発生した。原因はさくら側の海外アクセス制限等が疑われるが未確定。切り分けの手間を避けるため、SMTP中継はResendに変更した。
- **SPF/DKIM/DMARC：** Resendのダッシュボードでドメイン `family-closet.com` を追加すると必要なSPF/DKIM用のDNSレコード（TXT・CNAME等）が提示されるので、それをさくらインターネットのDNS管理画面上で `family-closet.com` に追加して検証する。

---

## 3. 全体レイアウト・共通UI仕様

### 3.1 ヘッダーエリア

- **ロゴ：** タップでトップ画面または一覧画面に遷移。
- **ユーザーアイコン（右上）：** ログイン中メンバーのメンバーIDの頭1文字を表示。タップするとドロップダウンメニューが開き、ログアウトが実行可能。

### 3.2 フッターナビゲーション（画面下部常駐ボトムメニュー）

フローティングボタン（FAB）は使用せず、画面下に常時表示されるナビゲーションを採用。

- **一覧：** 洋服一覧ダッシュボードへ遷移。
- **追加：** 洋服登録画面へ遷移。
- **設定：** ファミリー・メンバー設定画面へ遷移。

---

## 4. メンバー権限・役割定義

| ロール             | 対象                    | 主な権限・役割                               |
| ------------------ | ----------------------- | -------------------------------------------- |
| **代表者 (Owner)** | ファミリー作成者（1名） | ・ファミリー全体の契約・サブスク決済管理<br> |

<br>・メンバーの招待・削除・権限変更<br>

<br>・プラン変更（チェスト ↔ ウォークイン）<br>

<br>・全データの閲覧・編集・削除 |
| **一般メンバー (Member)** | 追加された家族 | ・洋服データの閲覧・登録・編集・削除<br>

<br>・自身のプロフィール情報変更<br>

<br>※契約変更や他メンバーの削除は不可 |

---

## 5. 全画面詳細仕様

### 5.1 画面①: LP 兼 ゲスト体験（Home / Top - `/`）

- **目的：** 初めてアクセスしたユーザーへのサービス説明、ログイン、および登録なしで試せる「フィッティングプラン（ゲスト利用）」の提供。
- **UI構成要素：**
- ヒーローセクション（サービス紹介、主要画面イメージ）
- メインCTAボタン：「まずはお試し（登録不要）」
- サブCTAボタン：「ファミリーを新規作成」
- 言語選択ドロップダウン（サブCTAボタンの直下に配置。選択肢は日本語を末尾にした並び順）
- 機能紹介ブロック（Vision AI自動タグ付け、家族共有のメリット、多言語対応）
- ログインフォームエリア（ファミリーID、メンバーID、パスワードの入力フィールドおよびログインボタン）
- フッター（利用規約、プライバシーポリシーのみ。言語選択ドロップダウンはCTA直下に移動済み）

- **動作・ロジック：**
- **ゲスト利用開始：** 「まずはお試し」タップ時、バックエンドで一時的なゲストIDを自動生成し、匿名セッション（有効期限14日間）を発行。即座に洋服一覧ダッシュボードへ遷移（画面上部に14日間/10着制限の案内バナー常時表示）。
- **ログイン処理：** 入力されたIDとパスワードで認証。初回ログインフラグがTrueの場合は画面③（初回パスワード変更）へ強制遷移。Falseの場合は画面④（洋服一覧）へ遷移。
- **セキュリティ：** 5回連続ログイン失敗で5分間ロックするレートリミットを適用。

### 5.2 画面②: ファミリー新規登録画面（代表者用 - `/register`）

- **目的：** ファミリーアカウントの立ち上げと代表者アカウントの作成。ゲストからの引き継ぎもここで実行。
- **UI構成要素：**
- 代表者メールアドレス入力欄（本人確認用）
- 代表者パスワード入力欄（8文字以上、英数字混合）および確認用入力欄
- 希望ファミリーID入力欄（半角英数字・ハイフンのみ、リアルタイム重複チェック表示付き）
- 代表者メンバーID入力欄（例: dad, taro）
- 代表者表示名入力欄（例: パパ）
- プラン選択ラジオボタン（チェストプラン / ウォークインプラン）
- 利用規約およびプライバシーポリシー同意チェックボックス
- 「お試し中のデータを引き継ぐ」チェックボックス（ゲスト利用時のみ表示）
- 「ファミリーを作成して始める」送信ボタン

- **動作・ロジック：**
- ファミリーIDのユニーク判定をリアルタイムで実行。
- ゲストセッションが存在する場合、保持している最大10着の洋服データの所有権（family_id）を作成したファミリーIDおよび代表者アカウントへ一括更新。
- 本人確認用メールを送信（送信元: `noreply@family-closet.com`。基盤は「2.3 メール送信基盤」を参照）。

### 5.3 画面③: 初回強制パスワード変更画面（`/change-password`）

- **目的：** 代表者が作成した家族メンバーや代表者の初回ログイン時におけるセキュリティ担保。
- **UI構成要素：**
- 案内テキスト（「初回ログインのため、パスワードの変更が必要です」）
- 現在のパスワード入力欄
- 新しいパスワード入力欄（8文字以上、英数字混合）
- 新しいパスワード（確認用）入力欄
- 「パスワードを変更して利用開始」ボタン

- **動作・ロジック：**
- パスワード変更が完了するまで、Middlewareレベルで他のすべての画面やAPIへのアクセスを遮断。更新完了後に初回ログインフラグをFalseにし、画面④（洋服一覧）へ遷移。

### 5.4 画面④: 洋服一覧ダッシュボード（`/dashboard`）

- **目的：** 登録された洋服の閲覧・検索・フィルタリング。
- **UI構成要素：**
- メンバー切り替えタブ（「全員」「長女」「ママ」など）
- 検索・絞り込みドロワー/フィルタバー：
- オーナー（家族メンバー）
- カテゴリ（コート、トップス、パンツ等）
- 使用状態（使用中、保管中、譲渡/廃棄予定）
- シーズン（通年、春、夏、秋、冬、春夏、秋冬）
- サイズ（70〜160、XS、S、M、L、XL、FREE等）
- フリーワード検索

- 洋服カードのグリッド表示（軽量サムネイル画像、カテゴリ、オーナー、サイズを表示）

- **動作・ロジック：**
- 画像は一覧用に最適化されたサムネイル（長辺約300px / 約20KB）を使用。
- スワイプに応じた遅延読み込み（Lazy Loading）とBlur-up表示を適用。

### 5.5 画面⑤: 洋服登録・編集画面（`/clothes/new`, `/clothes/[id]/edit`）

- **目的：** 洋服の新規撮影・アップロードと属性情報の登録および修正。
- **UI構成要素：**
- 写真撮影/ファイル選択エリア
- 画像プレビューおよび「90度回転ボタン（左回転 / 右回転）」
- AI解析ローディング表示（スケルトンアニメーション）
- 入力フォーム項目：
- 名前（AI自動補完、手動変更可）
- カテゴリ（AI自動補完、手動変更可）
- 色（AI自動補完、手動変更可）
- オーナー（家族メンバーから選択）
- 使用状態（使用中 / 保管中 / 譲渡・廃棄予定）
- シーズン（通年 / 春 / 夏 / 秋 / 冬 / 春夏 / 秋冬、AI自動補完、手動変更可）
- サイズ（70〜160 / XS / S / M / L / XL / FREE等）
- メモ欄

- 保存ボタン

- **動作・ロジック：**
- **回転機能：** プレビュー上で回転指示があった場合、その角度で画像データを補正した上でWebP変換およびリサイズを実行。
- **非同期UI：** 写真選択直後にローカル環境でプレビューを表示。裏側でGemini（`gemini-3.1-flash-lite`）APIを呼び出し、解析完了後に名前・カテゴリ・色・シーズンをフォームへ自動挿入。AIには現在の表示言語を渡し、返却値もその言語で受け取る（シーズンは内部的に固定の日本語キーへ変換して保持）。
- **アイテム数上限到達時：** 保存を試みてプラン上限（フィッティング10着 / チェスト50着）に達している場合、インラインのエラーテキストではなくモーダルを表示。ゲスト（フィッティング）の場合は「ファミリーを登録する」ボタンで`/register`（画面②）へ、登録済みメンバー（チェストの上限到達）の場合は「プランをアップグレード」ボタンで設定画面（画面⑦）のプラン確認・変更ブロックへ誘導する。

### 5.6 画面⑥: 洋服詳細モーダル / 画面（`/clothes/[id]`）

- **目的：** 登録した洋服の拡大表示、詳細確認、ステータス更新、削除。
- **UI構成要素：**
- 高解像度画像表示（有効期限付き署名URLで取得）
- 左右スワイプ導線（または左右矢印ボタン）
- 属性一覧（カテゴリ、色、オーナー、シーズン、サイズ、使用状態、登録日、メモ）
- ステータスワンタップ切り替えボタン（「使用中」「保管中」「譲渡/廃棄予定」）
- 編集ボタン、削除ボタン

- **動作・ロジック：**
- **スワイプ遷移：** 一覧画面で適用されていた検索・絞り込み結果の並び順に従い、左右スワイプで前後の洋服詳細へ切り替え。

### 5.7 画面⑦: ファミリー・メンバー設定画面（`/settings`）

- **目的：** アカウント情報の確認、家族メンバーの管理、言語設定、プラン確認・変更。
- **UI構成要素：**
- **アカウント情報カード：** ログインIDである「ファミリーID」「メンバーID」は画面上に一切表示しない。代わりに、自分で編集可能な「ファミリー名」と「メンバー」（自分の表示名）の2行を表示する。
  - ファミリー名のデフォルトは「ファミリー名」。ゲスト（フィッティングプラン）利用中は、作成時点の表示言語に応じた「ゲストファミリー」（en: Guest Family / 简体: 访客家庭 / 繁體: 訪客家庭）。
  - メンバー名のデフォルトは、メールアドレスの`@`より前の文字列。ゲスト利用中は作成時点の表示言語に応じた「ゲスト」（en: Guest / 简体: 访客 / 繁體: 訪客）。
  - いずれも各行の「編集」ボタンからインライン編集し、自分（ログイン中の代表者/メンバー）のファミリー名・自分の表示名のみ変更できる。他メンバーの表示名は変更できない。
- **家族メンバー管理ブロック（代表者のみ）：** 家族メンバーの一覧（表示名のみ、メンバーIDは表示しない）とロール（代表者/メンバー）を表示。メンバー追加は「QRコード表示」ボタンのみを表示する（QRコードの読み取り・招待完了までのフロー詳細は今後決定）。人数上限に達している場合は追加ボタンの代わりに上限案内テキストを表示。
- **言語切り替え設定：** 日本語 / English / 中文（簡体字）/ 中文（繁體字）を選択。選択肢は日本語を末尾に表示する。
- **フィッティング利用時案内バナー：** 正式プラン（チェスト / ウォークイン）への移行案内ボタン。
- **プラン・決済状況（代表者のみ）：** 現在のプラン表示（フィッティング / チェスト / ウォークイン）、アップグレード・解約手続き（Stripe / RevenueCat連携）。

---

## 6. 多言語対応仕様 (i18n - パターンB: URLプレフィックスなし)

- **対応言語：** 日本語 (ja)、英語 (en)、中国語 (zh-CN)
- **URL構成：** `/ja/` や `/en/` などの言語コードを含めず、全画面でシンプルなURL（`/`, `/dashboard`, `/register` 等）を維持。
- **言語判定ルール：**

1. **ログイン状態:** データベース（`members` テーブル）に保持されているユーザーの「優先言語設定（`preferred_language`）」を自動参照して表示。
2. **未ログイン / ゲスト時:** Cookie に保存された言語選択（`lang=ja` 等）を参照。Cookie が無い場合はブラウザの `Accept-Language` ヘッダーから自動判別。

---

## 7. 収益化・お試し・制限モデル

| プラン名                     | 区分                    | 月額料金 | メンバー制限                        | アイテム制限   | 利用期限 / 条件          | データ保持・移行仕様                                                                                                                         |
| ---------------------------- | ----------------------- | -------- | ----------------------------------- | -------------- | ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| **フィッティング (Fitting)** | お試し（ゲスト）        | 無料     | 1名（代表者のみ）                   | 最大 10 着     | 初回利用から **14 日間** | 14日経過後はチェストまたはウォークインへの移行が必須。未登録のまま14日経過でDB・ストレージから物理全削除（新規登録時にデータ引き継ぎ可能）。 |
| **チェスト (Chest)**         | 無料/エントリーサブスク | 月額定額 | **最大 5 名**（代表者1名＋一般4名） | **最大 50 着** | 無制限（サブスク継続中） | 正式ファミリーアカウントとしてデータを永続保持。制限超過時にウォークインへの昇格案内。                                                       |
| **ウォークイン (Walk-in)**   | プレミアムサブスク      | 月額定額 | **無制限**                          | **無制限**     | 無制限（サブスク継続中） | 人数・着数を気にせず家族全員の全アイテムを永続保持。                                                                                         |

---

## 8. データベースモデル定義 (`packages/database`)

共通パッケージに以下のエンティティを定義し、マルチテナント分離とデータ保護を実現します。

- **`families` (ファミリー):** テナント（家計）単位の管理。ログインID（`id`）とは別に、設定画面で表示・編集する「ファミリー名」（`name`）を保持。ゲストフラグおよびゲスト有効期限（初回アクセスから14日）を保持。
- **`members` (メンバー):** ファミリー内の個別利用ユーザー。Supabase `auth.users` と連携し、家族内での役割（`owner`: 代表者 / `member`: 一般メンバー）、初期パスワードフラグ、言語設定を保持。
- **`clothes` (洋服):** 洋服アイテム情報。画像URL、サムネイルURL、カテゴリ、色、サイズ、シーズン、ステータス（使用中・保管中・譲渡/廃棄予定）を保持。
- **`subscriptions` (サブスクリプション):** プラン状態（fitting / chest / walk_in）、Stripe顧客ID、契約ステータスを保持。
- **`audit_logs` (監査ログ):** 運営者（App Admin）による管理操作の改ざん不可履歴記録。

---

## 9. パフォーマンス & セキュリティ・ガバナンス要件

- **マルチテナント隔離 (RLS):** データベースレベルで `family_id` に基づく行レベルセキュリティを適用し、他ファミリーのデータ閲覧・改ざんを不可とする。
- **画像自動軽量化:** アップロード時に回転補正を行った上でWebP形式へ自動変換および長辺リサイズを行い、100〜200KB以下に圧縮。
- **ブルートフォース対策:** IP・アカウント単位で5分間5回失敗時にアカウントを一時ロックするレートリミットを導入。
- **多要素認証 (2FA):** App Admin運営者アカウントへのTOTP 2FAの必須化。
- **画像アクセス制御:** 画像バケットのパブリック公開を禁止し、有効期限付きの「署名付きURL（Signed URLs）」経由で配信。
- **データ完全削除:** 14日経過した未登録ゲストデータについて、データベースの定期バッチ処理 (`pg_cron`) により物理全削除。
- **プロンプトインジェクション対策:** Vision AIへの入力・出力に対してZodによる厳格な構造バリデーションを適用。
- **運営管理画面:** IP制限（Zero Trust）および全操作を記録する改ざん不可能な「監査ログ（Audit Logs）」を導入。
